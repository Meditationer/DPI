			浏览器内核可以分成：渲染引擎和JS引擎。
	 渲染引擎对网页语法的解释（如HTML、JavaScript）并渲染网页。一般指排版引擎
	 JS引擎一个专门处理JavaScript脚本的虚拟机
js-css/heml 框架或者库：
库只管引进来使用，而框架有着各种各样的严格约束。

		jquery		是js的库
		yui			是js的库
		bootstrap	属于HTML和CSS框架。（约束了DOM结构）。web端
		weui		UI 库，移动端
		vue			是一个 JavaScript 框架
		react		是一个 JavaScript 框架		（常用）
		angularjs	是一个 JavaScript 框架
		Gumby		CSS框架
		SASS		CSS框架

移动混合开发开发框架：（网页包装成app）
	基于h5技术，本质上是js引擎渲染（h5是移动网页自身）
	
前端框架作为 DSL
	weex			基于vue。不适用于小程序
	react native	基于react。不适用于小程序（方向和技术栈更贴近 React）
	h5+ app			传统h5 app调通过h5+ runtime调用原生接口达到效果的app

JavaScript为基础选定一个DSL框架（JavaScript 编译型）
	
	uni-app（六端开发，Vue.js 开发）。
			非H5端的js是运行在一个独立的js core或v8下，不能用浏览器专用的js API。
			要想组件全端通用，必须是基于vue且无dom操作的库、组件。
			结合了小程序，和自家h5+特性，用vue方式书写的跨平台解决方案，屏蔽dom操作	
			一套独立的条件编译语法，
			
	Taro（六端开发），业务逻辑中根据环境变量使用条件编译
	h5+ app：传统h5 app调通过h5+ runtime调用原生接口达到效果的app（Runtime模块管理运行环境）
	
纯正的的多端开发（自带渲染引擎）
			
	flutter（c++/c编写）。使用skia作为渲染引擎，渲染程度更好（更看重性能和跨平台的兼容）
			flutter的学习成本主要在Dart。DSL（QML/Dart）

微信小程序框架：chameleon、Taro、uni-app、mpvue、WePY

Android和iPhone的就像汽油机和柴油机
原生开发就像是在汽油机和柴油机外面套壳造车。
flutter是用了一套渲染引擎，就像是在汽油机和柴油机上接了发电机，从传统能源车变成了混动车。
h5技术栈上的产品，如h5+ app，uniapp就像他们用来烧水，用蒸汽作为动力。


node.js 就是运行在服务端的 JavaScript
		Express 	 node.js Web应用框架